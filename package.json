{
  "name": "uxmind",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "ingest": "tsx scripts/ingest.ts",
    "score": "tsx scripts/score-entry.ts"
  },
  "devDependencies": {
    "turbo": "^2",
    "tsx": "^4",
    "typescript": "^5"
  }
}
